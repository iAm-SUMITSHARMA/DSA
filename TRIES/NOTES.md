# TRIE DATA STRUCTURE
used to solve string(pattern matching problems) questions

-->> Trie also known as retrieval tree

## What is a Trie?
Trie is a k-ary(generic) tree

## Search in Trie
Tc: O(L) ; L is the length of the key
## INSERT in Trie

# Trie:
* Used for retrieval queries
* Used gen. for insert and search ops
* *Searches in O(L)* , L = key length but uses extra space
Tc: O(L) ; L is the length of the key