# Disconnected Components


# Cycle in Graphs

1. Undirected
There are various algorithms to detect cycle in undirected graph: 
--> *bfs
--> *dfs
--> *disjoint set union

2. Directed
There are various algorithms to detect cycle in directed graph: 
--> *bfs
--> *dfs
--> *Kahn's algorithm(Topological sort)